[project]
name = "conduit-opensky"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["conduit-etl"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra -q"
filterwarnings = ["ignore::DeprecationWarning"]
